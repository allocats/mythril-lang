# Mythril design plans 

Namespacing: Only force namespacing for symbols, ```allocate()``` vs ```std::memory::allocate()```, if there are collisions. Cause compiliation error with a message asking for clarification.
